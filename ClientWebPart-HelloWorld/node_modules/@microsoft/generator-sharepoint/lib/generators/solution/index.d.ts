import { BaseGenerator, IBaseOptions } from '../../common/BaseGenerator';
export interface ISharedOptions {
    solutionName: string;
    skipFeatureDeployment: boolean;
}
export interface ISolutionOptions extends IBaseOptions, ISharedOptions {
    environment: 'spo' | 'onprem';
}
export interface ISolutionContext extends ISharedOptions {
    libraryName: string;
    libraryId: string;
    environment: 'spo' | 'onprem';
}
export declare function composeWith(base: BaseGenerator<any, any>, options: ISolutionOptions): void;
export declare function defineOptions(generator: BaseGenerator<any, any>): void;
